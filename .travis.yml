language: php

php:
  - '7.1'

services:
  - mysql

addons:
  - mariadb: '10.2'

before_install:
  - composer install --no-dev
  - sudo mysql -e "use mysql; update user set authentication_string=PASSWORD('new!pasSW04d') where User='root'; update user set plugin='mysql_native_password';FLUSH PRIVILEGES;"
  - sudo mysql_upgrade
  - sudo service mysql restart
  - mysql -u root --password="new!pasSW04d" < installation/create_database.sql

script: phpunit tests

